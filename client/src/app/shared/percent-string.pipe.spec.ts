import { PercentStringPipe } from './percent-string.pipe';

describe('PercentStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentStringPipe();
    expect(pipe).toBeTruthy();
  });
});
